--- index.html
+++ index.html
@@ -7012,7 +7012,11 @@
                           if (result.success) {
                               const existingText = workorderData.value.conceptoDesc || '';
                               workorderData.value.conceptoDesc = existingText + (existingText ? "\n\n" : "") + result.transcription;
+
+                              const existingAudioText = workorderData.value.estructuracionAudioText || '';
+                              workorderData.value.estructuracionAudioText = existingAudioText + (existingAudioText ? "\n\n" : "") + result.transcription;
+
                               engineeringQuestions.value = result.data || '';

                               Swal.fire({
