# --- Núcleo del backend (necesario para arrancar api/main.py y correr la suite) ---
# Versiones mínimas verificadas: con estas pasan los 99 tests de pytest.
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.9
python-multipart>=0.0.9
supabase>=2.10
gspread>=6.1
google-auth>=2.35
mcp>=1.2

# --- Capa relacional (backend/, Fase 1) ---
# Solo hacen falta para el motor SQLAlchemy, que es el de produccion y el unico
# con transacciones reales. El motor PostgREST usa la biblioteca estandar, asi
# que `backend/` se importa y su suite corre aunque estas dos no esten.
sqlalchemy>=2.0
psycopg[binary]>=3.1

# --- Agentes de IA y utilidades (opcionales para el tracker) ---
# api/paperclip_agents.py, api/ai_utils.py, api/engineering_agent.py y la app
# de Streamlit. El backend del tracker arranca sin ellas, pero los tests de
# tests/test_text_agents.py y tests/test_ai_utils_ffmpeg.py sí las necesitan.
groq
langchain-groq
langchain-community
langchain-google-genai
langgraph
tavily-python
pypdf
ffmpeg-python
streamlit
