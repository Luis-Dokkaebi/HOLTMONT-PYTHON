# Dependencias para correr la suite de pruebas.
#   pip install -r requirements.txt -r requirements-dev.txt
#   ./run_tests.sh
#
# Las pruebas de extremo a extremo (tests/conftest.py) levantan api/main.py,
# asi que requirements.txt tambien hace falta.
pytest>=8.0
playwright>=1.40
# Escenarios Gherkin de tests/features/ (R2). El limite superior es deliberado:
# el salto de major cambia el analizador de .feature y con el las palabras clave
# en espanol, que es justo lo que estos escenarios necesitan estable.
pytest-bdd>=8.1,<9
# Lectura de los export .xlsx del Tracker: la usan `scripts/cargar_tracker_xlsx.py`,
# `scripts/sincronizar_terminadas.py` y sus dos pruebas. Faltaba desde 80dfaf7
# (2026-08-08) y la puerta de CI llevaba desde entonces en rojo por
# `ModuleNotFoundError: No module named 'openpyxl'` al recolectar, sin llegar
# nunca a correr la suite.
openpyxl>=3.1
# Conversión del DENUE (INEGI) a Parquet: la usan `scripts/convertir_denue_a_parquet.py`
# y su prueba. Llegan de todos modos como dependencia transitiva de `streamlit`,
# pero una herramienta no debe apoyarse en lo que otra arrastra sin querer.
# Fuera de `api/requirements.txt` a propósito: pandas + pyarrow + numpy pesan
# 276 MB instalados y el bundle de una serverless de Vercel tope en 250 MB.
pandas>=2.0
pyarrow>=14.0
