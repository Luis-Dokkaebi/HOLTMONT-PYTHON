__pycache__/
.pytest_cache/
uvicorn.log
.env
node_modules/
node_modules/
package.json
package-lock.json

# Obsidian
.obsidian/workspace
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
venv/
out_test
tests/.cdn_cache/

# Notas de Prework Order generadas en runtime (y por las pruebas)
Notas/Prework_Orders/

# Respaldos que deja scripts/normalizar_estatus.py antes de escribir.
# Son datos de produccion (folios y estatus reales): se quedan en la maquina
# que ejecuta la migracion, no en el repositorio.
scripts/respaldos/
.venv/

# Artefactos de las restricciones extremas (RESTRICCIONES_EXTREMAS.md).
# Reportes de cobertura y de mutacion: se regeneran en cada corrida, no se versionan.
.coverage
.coverage.*
htmlcov/
coverage.xml
.mutmut-cache
.ruff_cache/
.mypy_cache/

# Respaldo que deja `scripts/sincronizar_espejo_administrador.py` antes de
# escribir: es un volcado de datos de produccion, no codigo.
respaldo_espejo_*.json
